version: '3.8'

services:
  backend:
    build: ./apps/backend
    ports:
      - "8080:8080"
    env_file:
      - ./apps/backend/.env
    restart: unless-stopped

  frontend:
    build: ./apps/frontend-new
    ports:
      - "3000:3000"
    restart: unless-stopped
